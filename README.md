# TinyML_based_increamental_LSTM
TinyML-style logic-layer anomaly detection for industrial water systems using a lightweight incremental LSTM (SWaT training + WADI validation) with feature reduction, logic-derived labels, and CPU/RSS tracking.
